# NON-PRODUCTION ARCHIVE

This directory contains deprecated, unmaintained prototype code that previously used synthetic data and mock market simulators.

**STRICT DIRECTIVE**:
- This directory is **NOT** used in production.
- Do NOT run, deploy, or import from `legacy/backend/`.
- The sole production backend is `cryptoscope-ai/apps/api/main.py`.
